version: "3.1"

rules:

- rule: Say goodbye anytime the user says goodbye
  steps:
  - intent: exit
  - action: utter_exit

- rule: when user is not satisfied and angry
  steps:
  - intent: User angry
  - action: utter_angry
